<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/stepsImage"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:paddingTop="@dimen/padding"
        android:paddingBottom="@dimen/padding"
        android:paddingLeft="@dimen/padding"
        android:paddingRight="@dimen/padding"
        android:visibility="gone"
        app:layout_constraintLeft_toLeftOf="parent"
        tools:src="@drawable/user_placeholder"/>

    <TextView
        android:id="@+id/stepsShort"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/padding"
        android:paddingBottom="@dimen/padding"
        android:paddingLeft="@dimen/padding"
        android:paddingRight="@dimen/padding"
        android:textStyle="bold"
        app:layout_constraintLeft_toRightOf="@+id/stepsImage"
        app:layout_constraintBottom_toTopOf="@+id/stepsDescription"
        />

    <TextView
        android:id="@+id/stepsDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/padding"
        android:paddingBottom="@dimen/padding"
        android:paddingLeft="@dimen/padding"
        android:paddingRight="@dimen/padding"
        app:layout_constraintLeft_toRightOf="@+id/stepsImage"
        app:layout_constraintTop_toBottomOf="@+id/stepsShort"

        />

</android.support.constraint.ConstraintLayout>